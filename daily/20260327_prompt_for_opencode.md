Prompt for opencode 2026/03/27
===

## 投資判断の履歴参照

```markdown:prompt
# Background
- 現在、システムの投資判断のログを見ることができません。
- 管理者向けにダッシュボードを作成したいです。

# Result
- 投資判断の履歴のダッシュボードを作成して、管理者が確認できるようにすること。

# Request
- DBに保存されている情報やS3に保存されている情報を参照して、履歴を参照するための画面を生成してください。

# Restrict
- 可能な限り安価な方法で実現をしてください。
- 管理者（私）が確認できればどのような方法でも構いません。
    - 例えば、AWSコマンドをローカルで実行して画面を生成するなどの方法でも大丈夫です。
```

```markdown:prompt
# Background
- 作成してもらったダッシュボードを参照すると、結果が0です。
- localhostのDybamoDBを参照しているようです。

# Result
- ダッシュボードがAWSでホストされているDynamoDBを参照すること。

# Request
- ダッシュボードがAWSでホストされているDynamoDBを参照するように修正をしてください。
```

```markdown:prompt
# Background
- 作成してもらったダッシュボードを参照すると、結果が0です。

# Result
- 問題を特定し、解決すること。

# Request
- CloudWatchのログを参照して、問題を特定し、必要があれば修正を行い問題を解決してください。
```

Bedrockの問題については解決しているので無視してください。

```markdown:prompt
# Background
- 作成してもらったダッシュボードが参照できなくなりました。

# Result
- ダッシュボードが参照できること

# Request
- 現在のリソース状況を反映して、ダッシュボードを参照できるようにしてください。
```

手動での実行を行い、システムが稼働するかテストをしてください。

EventBrdigeの頻度はいくつになっていますか？

```markdown:prompt
# Background
- Tradeのテストをしたいです。

# Result
- Tradeが実際に実行されること

# Request
- 投資実行のしきい値を一時的に0.78にして実行をしてください。
```

しきい値を0.75にしてTrade

```markdown:prompt
# Background
- Trade（実取引）を成功させたいです。
- ダッシュボードが表示されなくなりました。

# Result
- Tradeが実際に実行されること
- ダッシュボードが表示されること。

# Request
- BinanceのAPIキーの再設定をします。
    - 必要な情報を教えて下さい。
    - どのキーが設定されているのか教えて下さい。
- ダッシュボード(`npm run dashboard`)の表示が成功するように調整をしてください。
```

→きちんと設定されています。APIキーのテストをすることは可能ですか？

```markdown:prompt
# Background
- このマシンからテストを行っています。
- このマシンのIPアドレスは許可されていませんでした。
- 先ほど、このマシンのIPアドレス（210.136.241.136）を許可しました。

# Result
- APIキーのテストを再実行すること

# Request
- APIキーのテストを再実行してください。
```

Futureは日本のbinanceでは有効になっていないので、仮想通貨のレバレッジ取引は無効にしてください。
## 不要なリソースの削除および可視化

```markdown:prompt
# Background
- システムはAWSでホストされており、リソースが存在するコストが掛かります。
- また、このシステムでかかるコストがどれくらいなのかわかりません。

# Result
- 使用されていないAWS リソースが削除されること。
- 当システムで使用しているリソースにタグをつけ、Cost Explorerでコストを視覚化できること。

# Request
- システムのステータスを見て、不要なリソースがあれば削除をしてください。
- 使用しているリソースにはタグをつけ、コストエクスプローラで確認できるようにしてください。

# Restrict
- AWSのベストプラクティスに従い作業をしてください。
```

再度確認して
